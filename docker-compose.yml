version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - API_KEY=${API_KEY}
      - API_URL=${API_URL}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
